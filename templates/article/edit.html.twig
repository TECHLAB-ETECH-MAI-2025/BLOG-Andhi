{% extends 'base.html.twig' %}

{% block title %}Modification article - Mon Blog{% endblock %}

{% block body %}
    <div class="mb-10">
        {{ 
            include(
                'components/header/_navbar.html.twig',
                {
                    'go_back': path('app_article_show', {id: article.id}),
                    'nav_title': "Modification de l'article"
                }
                )
        }}
    </div>

    {{ include('article/_form.html.twig', {'button_label': 'Modifier l\'article'}) }}

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
